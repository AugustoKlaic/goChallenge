# Go challenge

### How to run:

##### Run with

* At the folder where main.go is execute: ```go run .```

##### Send request with
* ```curl -F 'file=@/path/matrix.csv' "localhost:8080/~Operation~"```
